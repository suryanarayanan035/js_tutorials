// Number() -> Converts a given value to a number
const convertedNumber = Number(false);

/* parseFloat -> Convert a given value to a number
    It can convert decimal values to its actual representation
*/
const convertedFloat = parseFloat("45.67");

/**
 * ceil - Rounds the given decimal value to returns the next integer value
 */

const ceilValue = Math.ceil(5.6);

/**
 * floor - Rounds the given decimal value to returns the current integer part
 */
const floorValue = Math.ceil(5.6);

/**
 * pow(base,power) - Returns the result of multiplying the base , the power number of time
 */

const powerValue = Math.pow(10, 10);
